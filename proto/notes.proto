syntax = "proto3";

service Notes {
  rpc List (Void) returns (NoteListResponse);
}

// Entities
message Note {
  int32 id = 1;
  string title = 2;
  string description = 3;
}

message Void {}
message NoteListResponse {
   Note note = 1;
}